#include <stdio.h>

/*
 * main - go to display using puts
 *
 * Return: always 0
 */

int main(void) 
{
	const char *message = "\"Programming is like building a multilingual puzzle";
	puts(message);
	return (0);
}
