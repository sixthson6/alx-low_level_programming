#include <unistd.h>
#include "main.h"

/**
 * mul - multiply two num
 * @a: first num
 * @b: second num
 * Return: 0 (Success)
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
